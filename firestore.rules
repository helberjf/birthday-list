rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Local emulator only. Keep production access behind the API service account/IAM.
      allow read, write: if true;
    }
  }
}
